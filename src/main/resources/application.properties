# MongoDB Atlas configuration
spring.data.mongodb.uri=${MONGODB_URI}
spring.data.mongodb.database=ewalletdb

# JWT configuration
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# Use Railway dynamic port
server.port=${PORT:8080}
